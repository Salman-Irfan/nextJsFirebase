import { firebaseRealTimeDb } from '@/config/firebaseConfig/firebaseConfig';
import { ref, set } from 'firebase/database';
import React from 'react';

const Home = () => {

  return (
    <main>
      <h1>Hello Next with Firebase</h1>
      
    </main>
  );
}

export default Home